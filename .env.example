# Credenciais do banco de dados (lado do navegador).
# Os nomes começam com VITE_ para o build publicá-las no bundle — é a chave
# pública, feita para isso. Os nomes foram mantidos como estavam para não
# quebrar o que já está configurado na hospedagem.
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# Endereço base das imagens do sistema (logos de partido, capa do check-in).
# Aponte para um domínio próprio ou CDN para que o domínio de quem hospeda os
# arquivos não apareça no HTML da página. Vazio usa o endereço atual.
VITE_MEDIA_BASE_URL=""

# Mesmo caso, para os arquivos da marca (logo do topo e da tela de login).
VITE_BRAND_MEDIA_BASE_URL=""

# Chave da base externa de onde vêm as fichas de vínculo de cliente.
# Fica só no servidor (sem o prefixo VITE_, para não ir para o bundle público)
# e é usada pela função /api/base-externa. Configure nas variáveis de ambiente
# da hospedagem. O nome foi mantido para não quebrar o que já está no ar.
NEXUS_PUBLIC_API_KEY=""

# Chave da API do CCO, usada pela pesquisa de estabelecimentos e pela
# inteligência territorial (território e Censo). É a mesma chave para as duas
# famílias de rotas, gerada no CCO em Configurações › API e mostrada uma única
# vez. Fica só no servidor (sem o prefixo VITE_): quem fala com o CCO são as
# funções /api/estabelecimentos e /api/territorio, nunca o navegador.
CCO_API_KEY=""

# Chave do SerpApi, usada pela pesquisa "Pesquisar no mapa" (motor Google
# Maps). Fica só no servidor (sem o prefixo VITE_): quem fala com o SerpApi é
# a função /api/mapa-busca, nunca o navegador — cada pesquisa é cobrada, e uma
# chave no bundle é uma chave pública. Gerada no painel do SerpApi.
SERPAPI_API_KEY=""

# Chave da OpenAI, usada pela conferência de Tipo de Operação repetido.
# Antes de criar um tipo novo, o painel pergunta ao modelo se algum tipo que já
# existe quer dizer a mesma coisa ("Lixo" e "Lixo Acumulado"). Fica só no
# servidor (sem o prefixo VITE_): quem fala com a OpenAI é a função
# /api/tipo-operacao-parecido, nunca o navegador — cada chamada é cobrada, e
# uma chave no bundle é uma chave pública.
# Vazia, a conferência simplesmente não acontece e o cadastro segue como antes.
OPENAI_API_KEY=""

# Modelo usado pela conferência de tipo repetido E pelo NEO, o analista que
# escreve o relatório da missão (este precisa enxergar imagens). Os nomes mudam
# de uma versão para outra, e trocar de modelo não deveria pedir deploy — por
# isso sai daqui, e não do código. Vazio usa gpt-5.4-mini.
OPENAI_MODEL=""

# Modelo do "Gerar ícone com NEO", que desenha ícones para os Tipos de
# Operação. Separado do OPENAI_MODEL de propósito: trocar o modelo que escreve
# relatório não deve trocar, sem ninguém perceber, o que desenha ícone.
# Vazio usa gpt-5.4-mini. Usa a mesma OPENAI_API_KEY.
OPENAI_ICON_MODEL=""

# Modelo que transcreve os áudios do check-in antes de o NEO analisá-los.
# É outro modelo porque é outro trabalho: som para texto. Vazio usa whisper-1.
OPENAI_TRANSCRIBE_MODEL=""

# Endereço do cadastro pelo QR Code.
# O cadastro tem domínio próprio, separado do painel: quem lê o QR Code nunca vê
# o endereço da área administrativa. Vazio usa o domínio embutido no código.
VITE_SIGNUP_BASE_URL=""

# Endereço do painel da equipe (o link que vai para o celular do integrante).
# Domínio próprio, separado do painel administrativo. Vazio usa o domínio
# embutido no código.
VITE_TEAM_BASE_URL=""

# Endereço de saída para quem abre os domínios de acesso sem link (sem QR Code
# e sem o link da equipe). O administrador troca isso em tela, na janela de
# compartilhar; esta variável é só o valor inicial, usado quando nada foi
# configurado ainda.
VITE_FALLBACK_REDIRECT_URL=""

# Capa do cartão do cliente, na tela inicial. Vazio usa a imagem embutida.
VITE_CLIENT_CARD_COVER_URL=""

# Endereço do painel do Delta Operacional (o link que vai para o celular de
# quem planeja as operações). Vazio usa o mesmo domínio do painel da equipe
# (VITE_TEAM_BASE_URL). O cadastro pelo QR Code do Delta Operacional usa o
# domínio do cadastro (VITE_SIGNUP_BASE_URL).
VITE_OPERACIONAL_BASE_URL=""
